{
  "name": "article-tts-reader",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "test": "node scripts/test-tts-request.mjs && node scripts/test-extraction.mjs",
    "verify": "npm run build && node scripts/verify-build.mjs"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "jsdom": "^26.0.0"
  }
}
